import DefaultHead from "@/components/DefaultHead";

export default async function Head() {
  return (
    <DefaultHead
      title="Protocol Revenue"
      description="Find out how much Protocol Revenue Tokemak is bringing in"
    />
  );
}
