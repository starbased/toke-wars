import DefaultHead from "@/components/DefaultHead";

export default function Head() {
  return (
    <DefaultHead
      title="Rewards Emissions"
      description="See how Tokemak reward emissions are changing over time."
    />
  );
}
